ifneq ($(CONFIG_WPA_SUPPLICANT),)

ifneq ($(ESP_SUPPLICANT),)
CSRCS += driver_wext.c
endif

DEPPATH += --dep-path src/drivers
VPATH += :src/drivers
CFLAGS += ${shell $(INCDIR) "$(CC)" $(APPDIR)/wireless/wpa_supplicant/src/drivers}

endif
