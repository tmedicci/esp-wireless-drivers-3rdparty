ifneq ($(CONFIG_WPA_SUPPLICANT),)

CSRCS += eap_wsc_common.c

DEPPATH += --dep-path src/eap_common
VPATH += :src/eap_common
CFLAGS += ${shell $(INCDIR) "$(CC)" $(APPDIR)/wireless/wpa_supplicant/src/eap_common}

endif
